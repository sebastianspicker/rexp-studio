# Run the opt-in Relution Docker integration suite on demand and each week.
name: Relution Docker E2E

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 1"

permissions:
  contents: read

jobs:
  relution-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.34.5

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Docker Relution E2E
        timeout-minutes: 20
        run: pnpm test:e2e:relution

      - name: Capture Docker logs
        if: always()
        run: |
          docker compose -p rexp-studio-e2e -f tests/relution-docker/compose.yml logs --no-color > relution-e2e.log || true

      - name: Upload Docker logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: relution-e2e-logs
          path: relution-e2e.log
          if-no-files-found: ignore
          retention-days: 5
